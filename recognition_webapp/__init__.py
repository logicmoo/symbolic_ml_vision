"""Omega Vision recognition core.

Modules are imported flat (e.g. ``import pipelines``) at runtime because the swipl ``omega_vision``
pack and the standalone hosts add this directory to ``sys.path`` directly. This package marker
exists so the code is also installable with pip (see ../pyproject.toml).
"""
